# Blank Makefile, so that Travis will build correctly.
# Without some "makefile" file present, and if using
# a version of Go >=1.2, then the install step will
# be false.
# See: https://docs.travis-ci.com/user/languages/go/#Dependency-Management
